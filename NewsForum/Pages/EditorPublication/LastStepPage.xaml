<Page
    x:Class="NewsForum.Pages.EditorPublication.LastStepPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewsForum.Pages.EditorPublication"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ChildrenTransitions="{StaticResource BaseChildrenAnimation}">

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Frame Name="CurrentFrame"/>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal">
            <Button Tag="CreatePublication"
                    Style="{StaticResource BaseMenuButtonStyle}"
                    Content="Создать публикацию"
                    Margin="0 0 15 0"
                    Tapped="Button_Tapped"/>
            <Button Tag="Cancel"
                    Content="Отмена"
                    Style="{StaticResource BaseMenuButtonStyle}"
                    Tapped="Button_Tapped"/>
        </StackPanel>
        
        
    </Grid>
</Page>